//
// Created by Stephanie Achoa on 9/7/21.
//

#include <stdio.h>
#include "primes.h"

int primes() {

    // input a positive integer, 'N,'
    //   and then proceeds to print the first N prime numbers.
    //Also, as soon as the test
    //finds that a number is divisible by another number, the test terminates
    //since it is then known that the number cannot be prime.


    // positive, bigger than zero, and integer (ignores number after the decimal)

  // // if (size > 0 && ){
//
  //  }
  //  else{
  //      printf("Enter a positive  \n");
  //  }
  return 0;
}